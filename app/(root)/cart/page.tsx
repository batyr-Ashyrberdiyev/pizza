import React from "react";

import { Header } from "@/components/Header";
import { Status } from "@/components/Status";

const Cart = () => {
  return (
    <div className="inner-container">{[] ? <div>dsasad</div> : <Status />}</div>
  );
};

export default Cart;
